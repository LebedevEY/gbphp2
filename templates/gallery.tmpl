<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="style.css">
    <title>Gallery</title>
</head>
<body>
    <div class="wrapper">
        <h1 class="header">Галерея</h1>
        <div class="content">
            {% for image in images %}
                <a href="fullsize.php/?img={{ image.name }}" target="_blank"><img width="200px" src="img/{{ image.name }}" alt=""></a>
            {% endfor %}
        </div>
    </div>

</body>
</html>